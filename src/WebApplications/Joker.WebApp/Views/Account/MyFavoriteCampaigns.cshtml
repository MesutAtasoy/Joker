@using Joker.WebApp.ViewComponents
@using Joker.WebApp.ViewModels
@using Joker.WebApp.ViewModels.Shared
@model List<Joker.WebApp.ViewModels.Favorite.FavoriteCampaignViewModel>
@{
    ViewData["Title"] = "My Stores";
    Layout = "_Layout";
}

<div class="row mt-5">
    <div class="col-lg-4">
        @await Html.PartialAsync("_AccountNav");
    </div>
    <div class="col-lg-8">
        <div class="row">
            <div class="col-lg-12">
                <div class="text-center mb-4 mt-4">
                    <h6 class="text-gradient text-primary">My Favorite Campaigns</h6>
                </div>
            </div>
        </div>

        <hr>
        @if (Model.Any())
        {
            <div class="row">
                <div class="row justify-content-center">
                    <div class="card">
                        <div class="table-responsive">
                            <table class="table align-items-center mb-0">
                                <thead>
                                <tr>
                                    <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Title</th>
                                </tr>
                                </thead>
                                <tbody>

                                @foreach (var favoriteCampaign in Model)
                                {
                                    <tr>
                                        <td>
                                            <div class="d-flex px-2 py-1">
                                                <div class="d-flex flex-column justify-content-center">
                                                    <h6 class="mb-0 text-xs">@favoriteCampaign.Campaign.Name</h6>
                                                </div>
                                            </div>
                                        </td>
                                    </tr>
                                }

                                </tbody>
                            </table>
                        </div>
                    </div>

                </div>


            </div>
        }
        else
        {
            <div class="text-center bg-light p-5">
                There is no favorite campaigns.
            </div>
        }
    </div>
</div>